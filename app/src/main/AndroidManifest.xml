<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".NetworkMonitorApp"
        android:allowBackup="true"
        android:icon="@android:drawable/stat_notify_sync_noanim"
        android:label="Network Monitor"
        android:roundIcon="@android:drawable/stat_notify_sync_noanim"
        android:supportsRtl="true"
        android:theme="@style/Theme.NetworkMonitor">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
